// Shared image request widths keep cards and detail views aligned. Place photo
// URLs are resolved from current Places metadata by PlacePhotoService; stored
// photo resource names are never used to make media requests.
export const PHOTO_WIDTH_RAIL = 400;
export const PHOTO_WIDTH_CARD = 600;
export const PHOTO_WIDTH_LARGE = 1200;
